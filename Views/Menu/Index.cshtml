@model IEnumerable<RestaurantWebsite.Models.Dish>
@{
    ViewData["Title"] = "Menu";
}

<div class="menu-page">
    <div class="container">
        <h1 class="text-center mb-4">Our Menu</h1>

        <!-- Filter form -->
        <div class="card shadow-sm p-3 mb-4">
            <form class="row g-3" method="get" asp-controller="Menu" asp-action="Index">
                <div class="col-md-4">
                    <label for="search" class="form-label">Search</label>
                    <input type="text" name="search" id="search" value="@ViewBag.Search" class="form-control" placeholder="Search dishes..." />
                </div>
                <div class="col-md-2">
                    <label for="minPrice" class="form-label">Min Price</label>
                    <input type="number" name="minPrice" id="minPrice" value="@ViewBag.MinPrice" class="form-control" step="0.01" />
                </div>
                <div class="col-md-2">
                    <label for="maxPrice" class="form-label">Max Price</label>
                    <input type="number" name="maxPrice" id="maxPrice" value="@ViewBag.MaxPrice" class="form-control" step="0.01" />
                </div>
                <div class="col-md-2 align-self-end">
                    <button type="submit" class="btn btn-primary w-100">Filter</button>
                </div>
            </form>
        </div>

        <div class="row g-4">
            @foreach (var dish in Model)
            {
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        @{
                            var imageUrl = string.IsNullOrEmpty(dish.ImageUrl)
                            ? Url.Content("~/images/menu.png")
                            : dish.ImageUrl;
                        }
                        <img src="@imageUrl" alt="@dish.Name" class="card-img-top" />
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@dish.Name</h5>
                            <p class="card-text">@dish.Description</p>
                            <div class="mt-auto">
                                <span class="fw-bold">@dish.Price.ToString("F2") $USD</span>
                                @if (dish.IsPopular)
                                {
                                    <span class="badge bg-warning text-dark ms-2">Popular</span>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>